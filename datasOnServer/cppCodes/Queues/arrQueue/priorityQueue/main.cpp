#include<iostream>
#include"queue.h"
using namespace std;

int main(){
    Queue q = init();
     enqueue(q,10,4); 
     enqueue(q,20,3); 
     enqueue(q,30,1); 
     enqueue(q,40,2); 
     display(q); 
     display(q); 
}
